version: 2.1

jobs:
  build:
    docker:
      - image: circleci/node:12.18.3
    steps:
      - checkout
      - run: yarn install
      - run: echo "EMPTY TESTS"

  deploy:
    docker:
      - image: circleci/node:12.18.3
    steps:
      - checkout
      - run: yarn install
      - run: echo "yarn deploy"

workflows:
  build_test_deploy:
    jobs:
      - build
      - deploy:
          requires:
            - build
